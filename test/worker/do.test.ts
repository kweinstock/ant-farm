// Under miniflare/workerd: alarm() advances the sim and reschedules; a connected
// WebSocket receives Hello + Snapshot then Diffs; snapshot is persisted to and
// reloaded from DO storage across a simulated eviction.

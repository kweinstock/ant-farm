// worker/inputs.ts: rejects non-food/water kinds, off-surface positions, and
// out-of-range amounts; enforces per-visitor daily allowance and global rate
// limit; queues accepted actions for the next tick only.

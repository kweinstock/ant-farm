// The above-ground strip: the single EXIT tile, visitor drop zones for food and
// water, and natural resource spawn points used by environment/season.ts.
// Exposes spawn scheduling hooks (season-driven) and the exit's current danger
// level (set by environment/hazards.ts, read by foraging.ts).

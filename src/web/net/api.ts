// fetch() wrappers for the REST endpoints (ants, ant detail, pins add/remove,
// lineage tree, roll of the dead, stats). Adds X-Visitor-Id header, JSON parse,
// error normalization, tiny in-memory cache for detail/tree lookups.

// Scrollable, paginated list of living ants (name, job, age) from
// /ant-farm/api/ants. Search/filter by job or lineage. Click -> selects the ant
// (store) and opens ant-card.ts; also centers the camera on it if on-screen.

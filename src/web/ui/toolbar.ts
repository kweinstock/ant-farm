// Tool palette: "Add food" / "Add water" (and a plain "Inspect" cursor). Selecting
// a tool then clicking a valid SURFACE tile calls net/api.ts actions endpoint.
// Shows cooldown / remaining daily allowance from water-meter.ts. Invalid targets
// are rejected client-side before the request.

// Detail panel for the selected ant: caste, job, age vs lifespan, energy,
// traits, parents (link into family-tree.ts), lineage crumb, and a PIN / UNPIN
// button (net/api.ts pins). Live fields refresh from the snapshot each tick.

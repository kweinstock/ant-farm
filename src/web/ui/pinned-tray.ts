// This visitor's pinned ants (from localStorage-backed pin set + server sync).
// Shows alive/deceased state; clicking jumps to the ant or its lineage. Drives
// the highlight ring in render/ants.ts.
